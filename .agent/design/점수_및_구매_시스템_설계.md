# 리듬 영어 학습 앱 - 점수 및 구매 시스템 설계

## 📋 목차
1. [시스템 개요](#시스템-개요)
2. [점수 체계](#점수-체계)
3. [데이터 구조](#데이터-구조)
4. [매니저 클래스 설계](#매니저-클래스-설계)
5. [UI/UX 흐름](#uiux-흐름)
6. [구현 로드맵](#구현-로드맵)

---

## 시스템 개요

### 핵심 목표
사용자가 **게임, 싱어롱, 테스트**를 플레이하면서 **포인트(코인)**를 획득하고, 이 포인트로 **새로운 노래(스텝)**를 구매하여 **플레이리스트**에 추가할 수 있는 경제 시스템 구축

### 주요 컴포넌트
```
┌─────────────────────────────────────────────────────┐
│              사용자 플레이 활동                       │
│  (Game1, Game2, SingAlong, StepTest, VocabTest)     │
└────────────────┬────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────┐
│           PointManager (점수 관리)                    │
│  • 점수 획득 로직                                     │
│  • 점수 차감 로직                                     │
│  • 점수 저장/로드                                     │
└────────────────┬────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────┐
│          ShopManager (구매 시스템)                    │
│  • 노래 목록 표시                                     │
│  • 구매 가능 여부 확인                                │
│  • 구매 처리                                         │
└────────────────┬────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────┐
│        PlaylistManager (플레이리스트)                 │
│  • 구매한 노래 목록                                   │
│  • 재생 순서 관리                                     │
│  • 즐겨찾기 기능                                      │
└─────────────────────────────────────────────────────┘
```

---

## 점수 체계

### 1. 점수 획득 시스템

#### 🎮 Game1 (리듬 게임)
```csharp
// 기본 공식
점수 = (성공한_노트_수 × 10) + (콤보_보너스) + (정확도_보너스)

// 콤보 보너스
- 10 콤보: +50 포인트
- 20 콤보: +100 포인트
- 50 콤보: +250 포인트
- 100 콤보: +500 포인트

// 정확도 보너스 (Perfect / Good / Bad 비율)
- Perfect 80% 이상: +200 포인트
- Perfect 60% 이상: +100 포인트
- Perfect 40% 이상: +50 포인트
```

#### 🎮 Game2 (리듬 데모)
```csharp
// 기본 공식
점수 = (성공_타이밍_수 × 15) + (완주_보너스)

// 완주 보너스
- 클리어: +100 포인트
- 풀콤보: +300 포인트
```

#### 🎤 SingAlong (따라 부르기)
```csharp
// 기본 공식
점수 = (통과한_라인_수 / 전체_라인_수) × 100

// 통과 기준: 60% 이상 단어 매칭
// 추가 보너스
- 1회 통과: 기본 점수
- 2회 이상 재시도 없이 통과: +50 포인트
- 전체 완료: +100 포인트
```

#### 📝 StepTest (문장 조립 테스트)
```csharp
// 기본 공식
점수 = (정답_수 / 전체_문제_수) × 150

// 추가 보너스
- 1회 정답: +20 포인트/문제
- 전체 정답: +200 포인트
- 오답 없이 완료: +300 포인트
```

#### 📚 VocabularyTest (단어 테스트)
```csharp
// 기본 공식
점수 = (정답_수 / 전체_문제_수) × 100

// 추가 보너스
- 5문제 연속 정답: +50 포인트
- 10문제 연속 정답: +100 포인트
- 전체 정답: +150 포인트
```

### 2. 점수 사용 시스템

#### 노래(스텝) 구매 가격 체계
```
┌──────────────┬──────────┬────────────────────┐
│   난이도      │  가격    │      설명           │
├──────────────┼──────────┼────────────────────┤
│ Beginner     │  300P   │ 기초 학습용 노래     │
│ Elementary   │  500P   │ 초급 노래           │
│ Intermediate │  800P   │ 중급 노래           │
│ Advanced     │ 1200P   │ 고급 노래           │
│ Expert       │ 1500P   │ 전문가급 노래        │
└──────────────┴──────────┴────────────────────┘
```

#### 특별 아이템 (선택사항)
```
┌────────────────┬──────────┬────────────────────┐
│     아이템      │  가격    │      효과           │
├────────────────┼──────────┼────────────────────┤
│ 힌트 구매      │   50P   │ 테스트 힌트 1회     │
│ 재시도 기회    │  100P   │ 오답 시 재도전      │
│ 더블 포인트    │  200P   │ 30분간 2배 점수     │
└────────────────┴──────────┴────────────────────┘
```

---

## 데이터 구조

### 1. UserPointsData (사용자 점수 데이터)
```csharp
[Serializable]
public class UserPointsData
{
    public int totalPoints;              // 총 누적 포인트
    public int availablePoints;           // 사용 가능 포인트
    public DateTime lastUpdated;          // 마지막 업데이트 시간
    
    public List<PointHistory> history;    // 포인트 획득/사용 내역
}

[Serializable]
public class PointHistory
{
    public DateTime timestamp;            // 시간
    public int amount;                    // 금액 (+ 획득, - 사용)
    public PointSource source;            // 획득 출처
    public string description;            // 설명
}

public enum PointSource
{
    Game1,
    Game2,
    SingAlong,
    StepTest,
    VocabularyTest,
    Purchase,                            // 구매로 인한 차감
    Bonus,                               // 보너스 지급
    Daily                                // 일일 출석 보너스
}
```

### 2. SongShopData (노래 상점 데이터)
```csharp
[Serializable]
public class SongShopData
{
    public List<SongItem> availableSongs;  // 구매 가능한 노래 목록
}

[Serializable]
public class SongItem
{
    public string songId;                  // 노래 ID (chapterId)
    public string title;                   // 노래 제목
    public string artist;                  // 아티스트
    public DifficultyLevel difficulty;     // 난이도
    public int price;                      // 가격
    public string thumbnailPath;           // 썸네일 이미지 경로
    public bool isPurchased;               // 구매 여부
    public DateTime? purchaseDate;         // 구매 날짜
    public string previewAudioPath;        // 미리듣기 오디오 경로
    public List<string> tags;              // 태그 (Pop, Rock, Ballad 등)
    
    // 노래 메타데이터
    public int stepCount;                  // 포함된 스텝 수
    public int estimatedPlayTime;          // 예상 플레이 시간 (초)
    public string description;             // 설명
}

public enum DifficultyLevel
{
    Beginner,      // 300P
    Elementary,    // 500P
    Intermediate,  // 800P
    Advanced,      // 1200P
    Expert         // 1500P
}
```

### 3. UserPlaylistData (사용자 플레이리스트 데이터)
```csharp
[Serializable]
public class UserPlaylistData
{
    public List<PlaylistItem> items;       // 플레이리스트 아이템
    public int currentIndex;               // 현재 재생 중인 인덱스
}

[Serializable]
public class PlaylistItem
{
    public string songId;                  // 노래 ID
    public int playCount;                  // 재생 횟수
    public bool isFavorite;                // 즐겨찾기 여부
    public DateTime addedDate;             // 추가된 날짜
    public DateTime? lastPlayedDate;       // 마지막 재생 날짜
    
    // 진행 상태
    public int completedSteps;             // 완료한 스텝 수
    public int totalSteps;                 // 총 스텝 수
    public float completionRate;           // 완료율 (0.0 ~ 1.0)
}
```

---

## 매니저 클래스 설계

### 1. PointManager (점수 관리자)

```csharp
/// <summary>
/// 포인트(코인) 시스템 관리
/// - 포인트 획득/차감
/// - 포인트 저장/로드
/// - 포인트 히스토리 관리
/// </summary>
public class PointManager
{
    private static PointManager _instance;
    public static PointManager Instance { get; }
    
    private UserPointsData pointsData;
    private JsonDataManager dataManager;
    
    // ========== 공개 API ==========
    
    /// <summary>
    /// 포인트 획득
    /// </summary>
    public void AddPoints(int amount, PointSource source, string description = "");
    
    /// <summary>
    /// 포인트 사용 (구매 등)
    /// </summary>
    public bool SpendPoints(int amount, string description = "");
    
    /// <summary>
    /// 현재 사용 가능 포인트 조회
    /// </summary>
    public int GetAvailablePoints();
    
    /// <summary>
    /// 총 누적 포인트 조회
    /// </summary>
    public int GetTotalPoints();
    
    /// <summary>
    /// 포인트 히스토리 조회
    /// </summary>
    public List<PointHistory> GetHistory(int maxCount = 50);
    
    // ========== 내부 메서드 ==========
    
    private void SavePointsData();
    private void LoadPointsData();
    private void RecordHistory(int amount, PointSource source, string description);
}
```

**구현 위치**: `Assets/Scripts/System/Economy/PointManager.cs`

---

### 2. ShopManager (상점 관리자)

```csharp
/// <summary>
/// 노래 상점 관리
/// - 구매 가능 노래 목록 표시
/// - 구매 처리
/// - 구매 상태 동기화
/// </summary>
public class ShopManager
{
    private static ShopManager _instance;
    public static ShopManager Instance { get; }
    
    private SongShopData shopData;
    private PointManager pointManager;
    private PlaylistManager playlistManager;
    
    // ========== 공개 API ==========
    
    /// <summary>
    /// 모든 구매 가능 노래 목록 반환
    /// </summary>
    public List<SongItem> GetAllSongs();
    
    /// <summary>
    /// 필터링된 노래 목록 반환
    /// </summary>
    public List<SongItem> GetFilteredSongs(
        DifficultyLevel? difficulty = null, 
        bool? purchasedOnly = null,
        string searchKeyword = ""
    );
    
    /// <summary>
    /// 노래 구매 시도
    /// </summary>
    public bool TryPurchaseSong(string songId, out string errorMessage);
    
    /// <summary>
    /// 구매 가능 여부 확인
    /// </summary>
    public bool CanPurchase(string songId);
    
    /// <summary>
    /// 특정 노래 정보 조회
    /// </summary>
    public SongItem GetSongInfo(string songId);
    
    // ========== 내부 메서드 ==========
    
    private void LoadShopData();
    private void SaveShopData();
    private void UnlockSong(string songId);
}
```

**구현 위치**: `Assets/Scripts/System/Economy/ShopManager.cs`

---

### 3. PlaylistManager (플레이리스트 관리자)

```csharp
/// <summary>
/// 사용자 플레이리스트 관리
/// - 구매한 노래 관리
/// - 재생 순서 관리
/// - 즐겨찾기 관리
/// </summary>
public class PlaylistManager
{
    private static PlaylistManager _instance;
    public static PlaylistManager Instance { get; }
    
    private UserPlaylistData playlistData;
    
    // ========== 공개 API ==========
    
    /// <summary>
    /// 플레이리스트에 노래 추가
    /// </summary>
    public void AddToPlaylist(string songId);
    
    /// <summary>
    /// 플레이리스트에서 노래 제거
    /// </summary>
    public void RemoveFromPlaylist(string songId);
    
    /// <summary>
    /// 플레이리스트 전체 조회
    /// </summary>
    public List<PlaylistItem> GetPlaylist();
    
    /// <summary>
    /// 즐겨찾기 토글
    /// </summary>
    public void ToggleFavorite(string songId);
    
    /// <summary>
    /// 재생 횟수 증가
    /// </summary>
    public void IncrementPlayCount(string songId);
    
    /// <summary>
    /// 진행도 업데이트
    /// </summary>
    public void UpdateProgress(string songId, int completedSteps, int totalSteps);
    
    /// <summary>
    /// 다음 재생할 노래 반환
    /// </summary>
    public PlaylistItem GetNextSong();
    
    // ========== 내부 메서드 ==========
    
    private void LoadPlaylistData();
    private void SavePlaylistData();
    private PlaylistItem GetPlaylistItem(string songId);
}
```

**구현 위치**: `Assets/Scripts/System/Economy/PlaylistManager.cs`

---

### 4. 기존 매니저 통합

#### Game1 통합
```csharp
// GameManager.cs
public void OnGameEnd()
{
    // 기존 로직...
    
    // 점수 계산
    int baseScore = successfulNotes * 10;
    int comboBonus = CalculateComboBonus();
    int accuracyBonus = CalculateAccuracyBonus();
    int totalPoints = baseScore + comboBonus + accuracyBonus;
    
    // PointManager에 점수 추가
    PointManager.Instance.AddPoints(
        totalPoints, 
        PointSource.Game1, 
        $"Game1 Clear - {currentSongTitle}"
    );
    
    Debug.Log($"[Game1] Earned {totalPoints} points!");
}
```

#### SingAlongManager 통합
```csharp
// SingAlongManager.cs - FinishAll() 메서드
private void FinishAll()
{
    // 기존 로직...
    
    // 점수 계산
    float completionRate = (float)passedCount / lines.Count;
    int baseScore = Mathf.RoundToInt(completionRate * 100);
    int bonus = (completionRate >= 0.9f) ? 100 : 0;
    int totalPoints = baseScore + bonus;
    
    // PointManager에 점수 추가
    PointManager.Instance.AddPoints(
        totalPoints,
        PointSource.SingAlong,
        $"SingAlong - Passed {passedCount}/{lines.Count} lines"
    );
    
    ui?.ShowFinishPanel(passedCount, lines.Count, totalPoints);
}
```

#### StepTestManager 통합
```csharp
// StepTestManager.cs - AllTestsComplete() 메서드
private void AllTestsComplete()
{
    // 점수 계산
    int correctCount = items.Count - _nextLoopIndices.Count;
    float accuracy = (float)correctCount / items.Count;
    int baseScore = Mathf.RoundToInt(accuracy * 150);
    
    int bonus = 0;
    if (accuracy == 1.0f) bonus = 300;  // 전체 정답
    else if (accuracy >= 0.8f) bonus = 200;
    
    int totalPoints = baseScore + bonus;
    
    // PointManager에 점수 추가
    PointManager.Instance.AddPoints(
        totalPoints,
        PointSource.StepTest,
        $"StepTest - {correctCount}/{items.Count} correct"
    );
    
    Debug.Log($"[StepTest] Earned {totalPoints} points!");
}
```

#### VocabularyTestManager 통합
```csharp
// VocabularyTestManager.cs - OnTestComplete() 메서드
private void OnTestComplete()
{
    // 점수 계산
    float accuracy = (float)correctCount / totalQuestions;
    int baseScore = Mathf.RoundToInt(accuracy * 100);
    
    int bonus = 0;
    if (consecutiveCorrect >= 10) bonus = 100;
    else if (consecutiveCorrect >= 5) bonus = 50;
    
    if (accuracy == 1.0f) bonus += 150;
    
    int totalPoints = baseScore + bonus;
    
    // PointManager에 점수 추가
    PointManager.Instance.AddPoints(
        totalPoints,
        PointSource.VocabularyTest,
        $"VocabTest - {correctCount}/{totalQuestions} correct"
    );
}
```

---

## UI/UX 흐름

### 1. 메인 메뉴 UI 구조

```
┌────────────────────────────────────────────┐
│          Main Menu                         │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │  💰 Points: 1,250 P   [+] 내역보기   │ │ <- 포인트 표시
│  └──────────────────────────────────────┘ │
│                                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐│
│  │  🎮      │  │  🎤      │  │  📝      ││
│  │  게임    │  │ 싱어롱    │  │  테스트  ││
│  └──────────┘  └──────────┘  └──────────┘│
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │  🛒 Shop - 새로운 노래 구매하기      │ │ <- 상점 진입
│  └──────────────────────────────────────┘ │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │  📁 My Playlist (12곡)                │ │ <- 플레이리스트
│  └──────────────────────────────────────┘ │
└────────────────────────────────────────────┘
```

---

### 2. Shop UI (상점)

```
┌────────────────────────────────────────────┐
│  🛒 Song Shop          💰 Available: 1250P │
├────────────────────────────────────────────┤
│  [All] [Beginner] [Elementary] [Advanced] │ <- 필터
│  [🔍 Search...]                            │
├────────────────────────────────────────────┤
│  ┌──────────────────────────────────────┐ │
│  │ 🎵 Shape of You - Ed Sheeran         │ │
│  │ ⭐⭐⭐ Intermediate                   │ │
│  │ 💰 800P  [▶ Preview]  [🛒 Purchase]  │ │
│  └──────────────────────────────────────┘ │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │ 🎵 Hello - Adele                     │ │
│  │ ⭐⭐⭐⭐ Advanced                     │ │
│  │ 💰 1200P [▶ Preview]  [🛒 Purchase]  │ │
│  └──────────────────────────────────────┘ │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │ 🎵 Wonderwall - Oasis  ✅ OWNED      │ │
│  │ ⭐⭐ Elementary                       │ │
│  │ [▶ Play Now]                         │ │
│  └──────────────────────────────────────┘ │
└────────────────────────────────────────────┘
```

---

### 3. Playlist UI (플레이리스트)

```
┌────────────────────────────────────────────┐
│  📁 My Playlist                            │
├────────────────────────────────────────────┤
│  [All Songs] [Favorites ⭐] [Progress]     │ <- 탭
├────────────────────────────────────────────┤
│  ┌──────────────────────────────────────┐ │
│  │ ⭐ Shape of You                      │ │
│  │ Progress: ████████░░ 80%              │ │
│  │ Played 12 times                       │ │
│  │ [▶ Play]  [Remove]                   │ │
│  └──────────────────────────────────────┘ │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │ Wonderwall                           │ │
│  │ Progress: ███░░░░░░░ 30%              │ │
│  │ Played 3 times                        │ │
│  │ [▶ Play]  [⭐ Favorite]              │ │
│  └──────────────────────────────────────┘ │
└────────────────────────────────────────────┘
```

---

### 4. Point History UI (포인트 내역)

```
┌────────────────────────────────────────────┐
│  💰 Point History                          │
│  Available: 1,250P | Total Earned: 5,830P │
├────────────────────────────────────────────┤
│  2025-12-25 08:15                          │
│  🎮 Game1 Clear - Shape of You             │
│  +320 P                                    │
├────────────────────────────────────────────┤
│  2025-12-25 07:42                          │
│  🛒 Purchase - Hello (Adele)               │
│  -1200 P                                   │
├────────────────────────────────────────────┤
│  2025-12-25 07:30                          │
│  🎤 SingAlong - Passed 18/20               │
│  +190 P                                    │
├────────────────────────────────────────────┤
│  2025-12-24 22:10                          │
│  📝 StepTest - 15/15 correct               │
│  +450 P                                    │
└────────────────────────────────────────────┘
```

---

### 5. 구매 확인 팝업

```
┌────────────────────────────────────────────┐
│  🛒 Purchase Confirmation                  │
├────────────────────────────────────────────┤
│                                            │
│  🎵 Shape of You - Ed Sheeran              │
│  ⭐⭐⭐ Intermediate                       │
│                                            │
│  Price: 💰 800P                            │
│  Your Balance: 💰 1,250P                   │
│  After Purchase: 💰 450P                   │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │ ✓ 구매하면 플레이리스트에 추가됩니다  │ │
│  │ ✓ 모든 스텝을 플레이할 수 있습니다    │ │
│  └──────────────────────────────────────┘ │
│                                            │
│  [Cancel]              [💰 Purchase]       │
└────────────────────────────────────────────┘
```

---

### 6. 구매 성공 팝업

```
┌────────────────────────────────────────────┐
│  🎉 Purchase Successful!                   │
├────────────────────────────────────────────┤
│                                            │
│  "Shape of You" has been added to          │
│  your playlist!                            │
│                                            │
│  💰 Remaining Balance: 450P                │
│                                            │
│  [View Playlist]        [Play Now]         │
└────────────────────────────────────────────┘
```

---

## 구현 로드맵

### Phase 1: 기반 시스템 구축 (1-2주)

#### Week 1-1: 데이터 구조 및 PointManager
- [ ] 데이터 구조 정의
  - `UserPointsData.cs`
  - `PointHistory.cs`
  - `PointSource.cs` (enum)
  
- [ ] `PointManager.cs` 구현
  - Singleton 패턴
  - `AddPoints()` 메서드
  - `SpendPoints()` 메서드
  - `GetAvailablePoints()` 메서드
  - JSON 저장/로드 로직
  
- [ ] 기존 매니저에 PointManager 통합
  - `GameManager.cs` (Game1)
  - `SingAlongManager.cs`
  - `StepTestManager.cs`
  - `VocabularyTestManager.cs`

#### Week 1-2: ShopManager 및 PlaylistManager
- [ ] 상점 데이터 구조
  - `SongShopData.cs`
  - `SongItem.cs`
  - `DifficultyLevel.cs` (enum)
  - `shop_catalog.json` (상점 노래 목록)

- [ ] `ShopManager.cs` 구현
  - `GetAllSongs()` 메서드
  - `GetFilteredSongs()` 메서드
  - `TryPurchaseSong()` 메서드
  - `CanPurchase()` 메서드

- [ ] `PlaylistManager.cs` 구현
  - `AddToPlaylist()` 메서드
  - `RemoveFromPlaylist()` 메서드
  - `GetPlaylist()` 메서드
  - `ToggleFavorite()` 메서드
  - `UpdateProgress()` 메서드

---

### Phase 2: UI 구현 (1-2주)

#### Week 2-1: 메인 메뉴 및 포인트 표시
- [ ] 메인 메뉴 UI 수정
  - 포인트 표시 위젯 추가
  - Shop 버튼 추가
  - Playlist 버튼 추가

- [ ] Point History UI
  - `PointHistoryView.uxml`
  - `PointHistoryView.uss`
  - `PointHistoryViewController.cs`

#### Week 2-2: Shop UI
- [ ] Shop UI 구현
  - `ShopView.uxml`
  - `ShopView.uss`
  - `ShopViewController.cs`
  - Song Card 컴포넌트
  - 필터 기능
  - 검색 기능
  - Preview 재생 기능

- [ ] 구매 확인 팝업
  - `PurchaseConfirmPopup.uxml`
  - `PurchaseConfirmPopup.uss`

---

### Phase 3: Playlist UI 및 통합 (1주)

#### Week 3: Playlist UI 및 최종 통합
- [ ] Playlist UI 구현
  - `PlaylistView.uxml`
  - `PlaylistView.uss`
  - `PlaylistViewController.cs`
  - Playlist Item 컴포넌트
  - 즐겨찾기 기능
  - 진행도 표시

- [ ] 통합 테스트
  - 게임 플레이 → 점수 획득 → Shop 구매 → Playlist 추가 전 과정 테스트
  - 데이터 저장/로드 안정성 테스트
  - UI/UX 흐름 검증

- [ ] 밸런싱
  - 점수 획득량 조정
  - 노래 가격 조정
  - 사용자 피드백 반영

---

### Phase 4: 추가 기능 (선택사항)

#### 보너스 기능
- [ ] 일일 출석 보너스 (Daily Login Reward)
  - 연속 출석 시 포인트 지급
  - 출석 체크 UI

- [ ] 업적 시스템 (Achievements)
  - 특정 조건 달성 시 포인트 지급
  - 업적 리스트 UI

- [ ] 특별 세일 시스템
  - 주간 할인 노래
  - 시간 제한 이벤트

- [ ] 포인트 선물하기
  - 친구에게 포인트 전송 (소셜 기능)

---

## 파일 구조

```
Assets/
├── Scripts/
│   ├── System/
│   │   ├── Economy/
│   │   │   ├── PointManager.cs          ✨ 새로 생성
│   │   │   ├── ShopManager.cs           ✨ 새로 생성
│   │   │   ├── PlaylistManager.cs       ✨ 새로 생성
│   │   │   └── Data/
│   │   │       ├── UserPointsData.cs    ✨ 새로 생성
│   │   │       ├── PointHistory.cs      ✨ 새로 생성
│   │   │       ├── SongShopData.cs      ✨ 새로 생성
│   │   │       ├── SongItem.cs          ✨ 새로 생성
│   │   │       └── UserPlaylistData.cs  ✨ 새로 생성
│   │   │
│   │   ├── UI/
│   │   │   ├── ShopViewController.cs    ✨ 새로 생성
│   │   │   ├── PlaylistViewController.cs ✨ 새로 생성
│   │   │   └── PointHistoryViewController.cs ✨ 새로 생성
│   │   │
│   │   ├── Game1/
│   │   │   └── GameManager.cs           🔧 수정
│   │   ├── SingAlong/
│   │   │   └── SingAlongManager.cs      🔧 수정
│   │   ├── Test/
│   │   │   └── StepTestManager.cs       🔧 수정
│   │   └── VocabularyTest/
│   │       └── VocabularyTestManager.cs 🔧 수정
│   │
├── Resources/
│   ├── Data/
│   │   ├── user_points.json             ✨ 생성됨 (런타임)
│   │   ├── user_playlist.json           ✨ 생성됨 (런타임)
│   │   └── shop_catalog.json            ✨ 새로 생성
│   │
│   └── UI/
│       ├── Shop/
│       │   ├── ShopView.uxml            ✨ 새로 생성
│       │   ├── ShopView.uss             ✨ 새로 생성
│       │   ├── PurchaseConfirmPopup.uxml ✨ 새로 생성
│       │   └── PurchaseConfirmPopup.uss  ✨ 새로 생성
│       │
│       ├── Playlist/
│       │   ├── PlaylistView.uxml        ✨ 새로 생성
│       │   └── PlaylistView.uss         ✨ 새로 생성
│       │
│       └── PointHistory/
│           ├── PointHistoryView.uxml    ✨ 새로 생성
│           └── PointHistoryView.uss     ✨ 새로 생성
```

---

## 핵심 설계 결정 사항

### 1. 점수 획득 시점
- ✅ **활동 완료 시점**: 게임/테스트 완료 시 즉시 지급
- ✅ **누적 방식**: 총 누적 포인트와 사용 가능 포인트 분리 관리
- ✅ **히스토리 보존**: 모든 획득/사용 내역을 기록하여 투명성 확보

### 2. 노래 구매 시스템
- ✅ **Chapter 단위 구매**: 개별 스텝이 아닌 Chapter(노래) 전체를 구매
- ✅ **영구 소유**: 한 번 구매하면 영구적으로 소유
- ✅ **플레이리스트 자동 추가**: 구매 시 자동으로 플레이리스트에 추가

### 3. 데이터 저장
- ✅ **로컬 JSON**: 현재는 로컬 JSON 파일로 저장
- ⏳ **클라우드 백업**: 추후 Firebase/PlayFab 등으로 확장 가능하도록 설계
- ✅ **Repository 패턴**: 저장소 교체 시 최소한의 수정만으로 대응 가능

### 4. 보안 및 검증
- ✅ **서버 검증 없음**: 현재는 클라이언트 기반 (오프라인 앱)
- ⚠️ **해킹 가능성**: JSON 파일 직접 수정 가능 (교육용 앱이므로 허용)
- ⏳ **추후 개선**: 서버 연동 시 서버 검증 추가 가능

---

## 예상 질문 및 답변

### Q1: 무료 노래는 어떻게 제공하나요?
**A**: 기본적으로 Beginner 챕터 1-3개를 무료로 제공하고, `shop_catalog.json`에서 `price: 0`으로 설정하면 됩니다.

### Q2: 포인트를 실제 돈으로 구매할 수 있나요?
**A**: 현재 설계에는 없지만, 추후 In-App Purchase 연동 시 `AddPoints()` 메서드에 `PointSource.IAP` 추가하면 됩니다.

### Q3: 노래를 환불할 수 있나요?
**A**: 기본 설계에는 환불 기능이 없습니다. 필요 시 `ShopManager`에 `RefundSong()` 메서드 추가 가능합니다.

### Q4: 점수 밸런싱 기준은 무엇인가요?
**A**: 
- 평균 플레이 시간: Game1 (3분) = 약 200-400P
- 노래 1곡 구매: 평균 800P = 약 2-3회 플레이 필요
- 사용자가 하루 1시간 플레이 시 약 2-3곡 구매 가능

### Q5: 플레이리스트 순서를 변경할 수 있나요?
**A**: 기본 설계에는 없지만, `PlaylistManager`에 `ReorderPlaylist()` 메서드 추가로 구현 가능합니다.

---

## 다음 단계

이 설계 문서를 검토하시고, 다음 사항을 결정해주세요:

1. **점수 밸런싱 승인**: 제안된 점수 체계가 적절한지?
2. **우선순위 결정**: Phase 1부터 순차적으로 진행할지, 특정 기능 우선 구현할지?
3. **추가 기능 필요 여부**: 일일 출석, 업적 시스템 등이 필요한지?
4. **UI 디자인 방향**: 다크 모드 유지, 색상 스킴 등?

결정되면 바로 구현을 시작하겠습니다! 🚀
